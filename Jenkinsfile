pipeline {
	agent any
	stages	{
		stage('Initiailize') {
			steps {
				echo "Testing.."
			}
		}
		stage('Build') {
			steps {
					echo 'Hello World!'
			}
		}
		stage('Deploy'){
			steps {
					echo 'Deploy'
			}
		}
	}
}
